class Kata {
  static getVolumeOfCuboid(length, width, height) {
    return length * width * height;
  }
}

// Cuboid formula solution in CodeWars.

cuboidFunction = (length, width, height) => {return length * width * height}
cuboidFunction(2, 2, 2)


// Arrow function cuboid formula.  Probably better.
